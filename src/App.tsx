import styled from 'styled-components'

function App() {

  return (
    <AppContainer>

    </AppContainer>
  )
}

const AppContainer = styled.div`
margin-inline: auto;
  padding: 24px;
  width: min(100%, 1440px);

  height: 50px;
  background:red;
`;

export default App;
